LLM에 "너 누구야"라는 질문은 의미가 없다. 모델은 지속적인 자아가 없다.
모델은 부팅되고, 토큰을 처리하고, 종료된다, 연속적인 자아 감각이 없다.

ChatGPT에 넌 누구냐고 하면 OpenAI가 GPT-3 모델을 기반으로 만들었다는 응답이 나오는데
이 것은 OpenAI와 ChatGpt에 관한 많은 자료들이 있기 때문에 그 것을 바탕으로 대답하는 것이다.

SFT 데이터셋에 이런 종류의 질문에 응답하는 형식을 추가함으로써 파인튜닝하면 
"저는 Allen Institute for Artificial Intelligence의 AI to에서 개발한 오픈 언어 모델입니다"
 와 같은 응답을 하게 할 수 있다. 

다른 방법으로는 컨텍스트 윈도우에 넣는 것이다.
대화 맨 처음에 시스템 메세지라는 특별한 메세지가 있는데, 여기엔 
"당신은 OpenAI가 개발한 모델이고 이름은 ChatGPT 4o이고, 이 날짜에 학습되었고, 지식 컷오프는 이것입니다"라고 하드코딩하고 상기시킬 수 있다.

따라서 인공지능은 인간과 같은 자기 인식