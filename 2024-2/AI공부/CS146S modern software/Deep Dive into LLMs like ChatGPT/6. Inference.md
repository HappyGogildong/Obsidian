![[Pasted image 20260201233519.png]]
그림과 같이 추론은 학습된 확률 분포를 기반으로 어떤 토큰이 주어졌을 때
**다음에 올 토큰을 출력**해주는 것이다.
당연히 확률을 기반으로 하기 때문에, 오른쪽 그림에서 287 11579 처럼 학습 데이터와 다른
시퀀스를 출력하기도 한다.
일종의 훈련 데이터의 리믹스라고 생각할 수도 있겠다.
이 과정을 반복하다 보면 훈련 문서의 스트림과는 다른 토큰 스트림을 생성하기 시작한다.

기본적으로 추론은 이 분포들에서 한 번에 하나씩 예측하고, 토큰을 계속 다시 입력하고 다음 것을 얻고, 항상 동전을 던지고 얼마나 운이 좋거나 나쁘냐에 따라 이 확률 분포에서 어떻게 샘플링하느냐에 따라 매우 다른 종류의 패턴을 얻을 수 있습니다. 이것이 추론입니다.

GPT와 같은 모델과 대화하는 것은 모두 추론이다.
훈련이 없고, 파라미터는 고정되어 있다.


하지만 대화 맥락을 기억하거나 사용자의 입력을 토대로 다른 응답을 하기도 하지 않나?
어떻게 이런 게 가능할까?
In Context Learning, RAG